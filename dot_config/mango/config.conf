# More option see https://github.com/DreamMaoMao/mango/wiki/

# Includes
source=~/.local/state/mango/theme.conf

# Disable window effects
blur=0
shadows=0
border_radius=0

# Disable Animations
animations=0
layer_animations=0
tag_animation_direction=0

# Tag default layouts
# tile,scroller,grid,deck,monocle,center_tile,vertical_tile,vertical_scroller
tagrule=id:1,layout_name:tile
tagrule=id:2,layout_name:scroller
tagrule=id:3,layout_name:tile
tagrule=id:4,layout_name:tile
tagrule=id:5,layout_name:tile
tagrule=id:6,layout_name:tile
tagrule=id:7,layout_name:tile
tagrule=id:8,layout_name:tile
tagrule=id:9,layout_name:scroller

# Tiling Layout settings
smartgaps=1
new_is_master=1
default_mfact=0.55
default_nmaster=1

# Scroller Layout Setting
scroller_structs=0
scroller_default_proportion=1.0
scroller_focus_center=1
scroller_prefer_center=1
edge_scroller_pointer_focus=1
scroller_default_proportion_single=1.0
scroller_proportion_preset=1.0,1.0,1.0

# Overview Setting
enable_hotarea=0
ov_tab_mode=1
overviewgappi=5
overviewgappo=30

# Misc
no_border_when_single=1
axis_bind_apply_timeout=100
focus_on_activate=1
idleinhibit_ignore_visible=1
sloppyfocus=1
warpcursor=1
focus_cross_monitor=0
focus_cross_tag=0
enable_floating_snap=0
snap_distance=30
cursor_size=24
cursor_hide_on_keypress=1
drag_tile_to_tile=1

# Keyboard
repeat_rate=25
repeat_delay=600
numlockon=0
xkb_rules_layout=de
xkb_rules_options=ctrl:nocaps

# Trackpad
disable_trackpad=0
tap_to_click=1
tap_and_drag=1
drag_lock=1
trackpad_natural_scrolling=1
disable_while_typing=1
left_handed=0
middle_button_emulation=1
swipe_min_threshold=1

# Mouse
mouse_natural_scrolling=0
mouse_accel_profile=2
mouse_accel_speed=-0.7
axis_scroll_factor=1.0

# Dimensions
borderpx=3
gappih=0
gappiv=0
gappoh=0
gappov=0

# Reload config
bind=SUPER+SHIFT,r,reload_config

# Menu and terminal
bind=SUPER,space,spawn,fuzzel
bind=SUPER,Return,spawn,footclient

# Exit Mango
bind=SUPER+SHIFT,m,quit

# Quit application
bind=SUPER+SHIFT,q,killclient,

# Switch window focus
bind=SUPER,k,focusdir,up
bind=SUPER,j,focusdir,down
bind=SUPER,h,focusdir,left
bind=SUPER,l,focusdir,right

# Swap window
bind=SUPER+SHIFT,k,exchange_client,up
bind=SUPER+SHIFT,j,exchange_client,down
bind=SUPER+SHIFT,h,exchange_client,left
bind=SUPER+SHIFT,l,exchange_client,right

# Switch window status
bind=SUPER,g,toggleglobal,
bind=SUPER,Tab,toggleoverview,
bind=SUPER+SHIFT,f,togglefullscreen,

# Scroller layout
bind=ALT,e,set_proportion,1.0
bind=ALT,x,switch_proportion_preset,

# Switch layouts
bind=SUPER+SHIFT,s,setlayout,scroller
bind=SUPER+SHIFT,t,setlayout,tile

# Switch to tag (workspace)
view_current_to_back=0

bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SUPER,5,view,5,0
bind=SUPER,6,view,6,0
bind=SUPER,7,view,7,0
bind=SUPER,8,view,8,0
bind=SUPER,9,view,9,0

# Move window to tag
bind=SUPER+SHIFT,1,tag,1,0
bind=SUPER+SHIFT,2,tag,2,0
bind=SUPER+SHIFT,3,tag,3,0
bind=SUPER+SHIFT,4,tag,4,0
bind=SUPER+SHIFT,5,tag,5,0
bind=SUPER+SHIFT,6,tag,6,0
bind=SUPER+SHIFT,7,tag,7,0
bind=SUPER+SHIFT,8,tag,8,0
bind=SUPER+SHIFT,9,tag,9,0

# Resize window
bind=SUPER+ALT,k,resizewin,+0,-50
bind=SUPER+ALT,j,resizewin,+0,+50
bind=SUPER+ALT,h,resizewin,-50,+0
bind=SUPER+ALT,l,resizewin,+50,+0

# Axis Bindings
axisbind=SUPER,UP,viewtoleft_have_client
axisbind=SUPER,DOWN,viewtoright_have_client

# Fn key binds for display brightness
bind=NONE,XF86MonBrightnessUp,spawn,brightnessctl set 5%+
bind=NONE,XF86MonBrightnessDown,spawn,brightnessctl set 5%-

# Fn key binds for audio control
bind=NONE,XF86AudioRaiseVolume,spawn,wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind=NONE,XF86AudioLowerVolume,spawn,wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind=NONE,XF86AudioMute,spawn,wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind=NONE,XF86AudioMicMute,spawn,wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# Screenshot binds
bind=SUPER,PRINT,spawn_shell, grim -g "$(slurp)" - | wl-copy && notify-send --app-name slurp "Slurp" "Screenshot copied to clipboard."
bind=SUPER+SHIFT,p,spawn_shell, grim -g "$(slurp)" - | wl-copy && notify-send --app-name slurp "Slurp" "Screenshot copied to clipboard."
bind=SUPER+SHIFT,PRINT,spawn_shell, grim -g "$(slurp)" -t ppm - | satty --filename - --fullscreen --output-filename "$HOME/Pictures/Screenshots/$(date '+%Y%m%d-%H:%M:%S').png"

# Environment variables
env=XCURSOR_THEME,Adwaita
env=XCURSOR_SIZE,24

# Window rules
windowrule=shield_when_capture:1,appid:footclient

# One-Shot executions
exec-once=foot --server
exec-once=nm-applet
exec-once=blueman-applet
exec-once=waybar
exec-once=systemctl --user start kanshi.service
exec-once=$HOME/.local/bin/swayidle.sh
